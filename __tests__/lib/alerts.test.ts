// TODO: Implement in M2.3 — RED commit before any implementation
// TDD target: shouldSendAlert() in lib/alerts.ts
import { describe, it } from 'vitest'

describe('alerts.ts', () => {
  describe('shouldSendAlert()', () => {
    it.todo('returns true when days to renewal equals threshold')
    it.todo('returns false when days to renewal does not match any threshold')
    it.todo('is deterministic when today is injected as parameter')
  })
})
