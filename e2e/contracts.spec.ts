// TODO: Implement E2E tests in Sprint 2+ (Playwright)
// Happy path: Login → Create Supplier → Create Contract → View Contract
import { test, expect } from '@playwright/test'

test.describe('Contracts E2E', () => {
  test.todo('happy path: login → create supplier → create contract → view contract')
  test.todo('member cannot delete contract')
  test.todo('admin can delete contract')
})
