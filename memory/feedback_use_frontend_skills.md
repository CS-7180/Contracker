---
name: Use ui-ux-pro-max for UI work; tdd and chart-guide for project-specific tasks
description: Frontend skills (animate-motion, design-system, frontend-audit, frontend-polish) were removed — use ui-ux-pro-max plugin instead for UI tasks
type: feedback
---

The four project-level frontend skills (`animate-motion`, `design-system`, `frontend-audit`, `frontend-polish`) have been removed. The `ui-ux-pro-max` plugin now covers general UI/UX tasks.

Remaining project skills:
- `/tdd` — Contracker-specific TDD red→green→refactor protocol (pure functions + API routes)
- `/chart-guide` — Recharts conventions for Contracker spend tracking page

For all UI work (animations, design tokens, component polish, accessibility audits) use the `ui-ux-pro-max` plugin.

**Why:** User removed the 4 frontend skill files on 2026-03-29 as they are now superseded by the `ui-ux-pro-max` plugin.

**How to apply:** When building or modifying UI, use `ui-ux-pro-max` for design intelligence. Use `/tdd` for TDD workflow and `/chart-guide` for Recharts work.
